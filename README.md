# regdelta
Agentic regulatory-change assistant for FDA food labeling — LangGraph agents over a two-tier retrieval stack (S3 Vectors + ephemeral OpenSearch Serverless) on AWS, built milestone-by-milestone with Claude Code and scored against a fixed golden eval set.
