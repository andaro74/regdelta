Closed unmerged, as staged. `ruling-cited` refused it with no bypass available to the repository owner (`bypass_actors: []`, `current_user_can_bypass: never`), and `unit` passed — so the block is attributable to the ground-truth gate alone, which is the whole point of filming it rather than describing it.

Recorded in milestones/M07/doors/.
